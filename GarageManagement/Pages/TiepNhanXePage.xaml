<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="GarageManagement.Pages.TiepNhanXePage"
    BackgroundColor="Bisque"
    Shell.NavBarIsVisible="False">

    <ScrollView Margin="20">
        <Frame CornerRadius="10" BorderColor="Black" BackgroundColor="AliceBlue" HasShadow="False" MaximumWidthRequest="800">
            <VerticalStackLayout Spacing="20">
                <Label TextColor="Black"
                    Text="TIẾP NHẬN XE MỚI"
                    FontSize="36"
                    FontAttributes="Bold"
                    HorizontalTextAlignment="Center" />

                <Label Text="Biển số" FontSize="Medium" TextColor="Black"/>
                <Frame
                    CornerRadius="10"
                    BorderColor="Black"
                    Padding="0"
                    HasShadow="False">
                    <Entry
                        x:Name="txtBienSo"
                        BackgroundColor="LightGray"
                        Placeholder="Nhập biển số xe"
                        Text="{Binding BienSo, Mode=TwoWay}"
                        TextColor="Black"
                        HorizontalOptions="Fill" />
                </Frame>

                <Label TextColor="Black" Text="Ngày tiếp nhận" FontSize="Medium" />
                <Frame
                    CornerRadius="10"
                    BorderColor="Black"
                    Padding="10,5"
                    BackgroundColor="LightGray"
                    HasShadow="False">
                    <DatePicker
                        x:Name="dpNgayTiepNhan"
                        Date="{Binding NgayTiepNhan, Mode=TwoWay}"
                        TextColor="Black"
                        HorizontalOptions="Fill" />
                </Frame>

                <StackLayout Spacing="15">

                    <Frame CornerRadius="10"
                        BorderColor="Gray"
                        Padding="15"
                        HasShadow="True"
                        BackgroundColor="White"
                        IsVisible="{Binding IsCarExists}">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="Xe đã tồn tại" FontAttributes="Bold" TextColor="Green"/>
                            <Label Text="{Binding TenXe, StringFormat='Tên xe: {0}'}" TextColor="Black"/>
                            <Label Text="{Binding TenHieuXe, StringFormat='Hiệu xe: {0}'}" TextColor="Black"/>
                            <Label Text="{Binding BienSo, StringFormat='Biển số: {0}'}" TextColor="Black"/>
                            <Button
                    Text="Chi tiết xe"
                    Command="{Binding ViewCarDetailsCommand}" />
                        </VerticalStackLayout>
                    </Frame>

                    <Frame
                        CornerRadius="10"
                        BorderColor="Gray"
                        Padding="15"
                        HasShadow="True"
                        BackgroundColor="White"
                        IsVisible="{Binding IsCarExists}">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="Thông tin chủ xe" FontAttributes="Bold" TextColor="Green"/>
                            <Label Text="{Binding TenChuXe, StringFormat='Họ và tên: {0}'}" TextColor="Black"/>
                            <Label Text="{Binding DiaChi, StringFormat='Địa chỉ: {0}'}" TextColor="Black"/>
                            <Label Text="{Binding SoDienThoai, StringFormat='Điện thoại: {0}'}" TextColor="Black"/>
                            <Button Text="Xem chi tiết chủ xe" Command="{Binding ViewOwnerDetailsCommand}" />
                        </VerticalStackLayout>
                    </Frame>

                    <Frame
                        CornerRadius="10"
                        BorderColor="Gray"
                        Padding="15"
                        HasShadow="True"
                        BackgroundColor="White"
                        IsVisible="{Binding IsCarNotFound}">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="Xe chưa có trong hệ thống" FontAttributes="Bold" TextColor="Red"/>
                            <Button
                                Text="Thêm xe mới"
                                Command="{Binding AddNewCarCommand}" />
                        </VerticalStackLayout>
                    </Frame>
                </StackLayout>
                
            </VerticalStackLayout>
        </Frame>
    </ScrollView>
</ContentView>
